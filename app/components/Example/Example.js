// import './styles.css';
if (process.env.BROWSER) require('./styles.css');
import { Link } from 'react-router';

const Example = () => {
  return (
    <div>

    </div>
  );
};

export default Example;